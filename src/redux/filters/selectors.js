export const selectFilters = state => state.filters.selectedFilters;

export const selectLocation = state => state.filters.location;

export const selectVehicleType = state => state.filters.vehicleType;
